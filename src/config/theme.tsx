import {ThemeConfig} from "antd";

export const theme = {
    token: {
        colorPrimary: '#4a79d8',
    }
} as ThemeConfig;


export const config = {
    theme: theme,
    title: 'Admin UI',
    logo: '/logo.png',
}

